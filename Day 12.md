### **File Handling :-**
* Find and replace => ```sed```
* Print column from a file => ```awk```

sed grep awk

Disable the selinux:-
```
/etc/selinux/config
```
yum install ansible

yum install gcc


